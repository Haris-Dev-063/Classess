/*// main div ko get kiya
var main = document.getElementById("main");

// phir humne is stage per unordered list get ki
var ul = document.createElement("ul");

// phir humne is stage per ul ke attribute add kia
ul.setAttribute("class", "ul");

// phir humne is stage per list item get ki
var li = document.createElement("li");

// phir humne is stage per li ke attribute add kia
li.setAttribute("class", "li");

// phir humne is stage per li ka text likha
var liTextNode = document.createTextNode("This is a list item!");

// phir humne is stage per li ke andar text add kia through appendChild ke
li.appendChild(liTextNode);

// phir humne is stage per ul ke andar li add kia through appendChild ke
ul.appendChild(li);
// phir humne is stage per main ke andar ul add kia through appendChild ke
main.appendChild(ul);*/

// var main = document.getElementById("main");

// console.log(main.parentNode.parentNode.parentNode);

